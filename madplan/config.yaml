name: "Madplan"
description: "AI-powered vegetarian family meal planner with weekly overview and chat interface."
version: "1.3.2"
slug: "madplan"
init: false
arch:
  - aarch64
  - amd64
  - armv7

ingress: true
ingress_port: 8001
ingress_stream: false

panel_icon: "mdi:food-variant"
panel_title: "Madplan"

ports:
  8001/tcp: 8001
ports_description:
  8001/tcp: "Madplan Web UI (direct access)"

options:
  openai_api_key: ""
  chat_model: "gpt-5.5"
  num_adults: 2
  num_children: 3
  salling_api_key: ""
  postal_code: ""

schema:
  openai_api_key: str
  chat_model: str
  num_adults: int
  num_children: int
  salling_api_key: str
  postal_code: str

startup: application
boot: auto
